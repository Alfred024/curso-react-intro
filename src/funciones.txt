//Función para filtrar las cards que solo incluyan un textp en específico, pero no funciona si solo rederizamos la card en un index específico
const movieType = movieList.filter(
  (movie) =>{
    return movie.Title.toLowerCase().includes(movieSearch.toLowerCase());
  }
);

//Función para agregar una película a favoritos (DEMO)


//Función para borrar una película de favoritos (DEMO)
const addFavouriteMovie = (movieName) =>{
    const newMovies = [...movieList];
    const movieIndex = newMovies.findIndex(
        (movie) => movie.Title == movieName;
    );
    const newMovies.splice(movieIndex, 1);
    setMovieList(newMovies);
}